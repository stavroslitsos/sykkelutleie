const encoder=new TextEncoder();
const bytes64=bytes=>btoa(Array.from(bytes,x=>String.fromCharCode(x)).join(''));
const from64=text=>Uint8Array.from(atob(text),x=>x.charCodeAt(0));
async function derive(password,salt){const material=await crypto.subtle.importKey('raw',encoder.encode(password),'PBKDF2',false,['deriveKey']);return crypto.subtle.deriveKey({name:'PBKDF2',salt,iterations:600000,hash:'SHA-256'},material,{name:'AES-GCM',length:256},false,['encrypt','decrypt']);}
export async function encryptKeys(data,password){if(typeof password!=='string'||password.length<12)throw new Error('Passordet må ha minst 12 tegn.');const salt=crypto.getRandomValues(new Uint8Array(16)),iv=crypto.getRandomValues(new Uint8Array(12));const key=await derive(password,salt);const cipher=await crypto.subtle.encrypt({name:'AES-GCM',iv},key,encoder.encode(JSON.stringify(data)));return{format:'nordlys.keys.v1',salt:bytes64(salt),iv:bytes64(iv),data:bytes64(new Uint8Array(cipher))};}
export async function decryptKeys(payload,password){if(payload?.format!=='nordlys.keys.v1'||typeof payload.data!=='string'||payload.data.length>1500000)throw new Error('Ugyldig filformat.');const salt=from64(payload.salt),iv=from64(payload.iv);if(salt.length!==16||iv.length!==12)throw new Error('Ugyldig filformat.');const key=await derive(password,salt);const plain=await crypto.subtle.decrypt({name:'AES-GCM',iv},key,from64(payload.data));return JSON.parse(new TextDecoder().decode(plain));}
