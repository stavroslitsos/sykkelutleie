const form=document.querySelector('#contactForm'),status=document.querySelector('#status'),button=form.querySelector('button');form.addEventListener('submit',async e=>{e.preventDefault();button.disabled=true;status.textContent=status.dataset.send;status.style.color='';try{const response=await fetch('https://api.web3forms.com/submit',{method:'POST',headers:{Accept:'application/json'},body:new FormData(form)}),result=await response.json();if(!result.success)throw new Error();status.textContent=status.dataset.ok;status.style.color='#168d6b';form.reset()}catch{status.textContent=status.dataset.error;status.style.color='#b2382d'}finally{button.disabled=false}});document.querySelector('#year').textContent=new Date().getFullYear();
